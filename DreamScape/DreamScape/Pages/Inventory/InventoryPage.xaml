<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="DreamScape.Pages.Inventory.InventoryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DreamScape.Pages.Inventory"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Transparent">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   x:Name="HeaderTextBlock"
                   FontSize="20"
                   FontWeight="SemiBold"
                   Margin="0,0,0,16"/>

        <ListView Grid.Row="1"
                  x:Name="InventoryListView"
                  SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="4" ColumnSpacing="16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Spacing="2">
                            <TextBlock Text="{Binding Item.Name}"
                                       FontWeight="SemiBold"
                                       FontSize="16"/>
                            <TextBlock Text="{Binding Item.Description}"
                                       Opacity="0.7"
                                       TextWrapping="Wrap"/>
                            <StackPanel Orientation="Horizontal" Spacing="12" Margin="0,4,0,0">
                                <TextBlock FontSize="12" Opacity="0.8">
                                    <Run Text="STR "/><Run Text="{Binding Item.Strength}"/>
                                </TextBlock>
                                <TextBlock FontSize="12" Opacity="0.8">
                                    <Run Text="SPD "/><Run Text="{Binding Item.Speed}"/>
                                </TextBlock>
                                <TextBlock FontSize="12" Opacity="0.8">
                                    <Run Text="DUR "/><Run Text="{Binding Item.Durability}"/>
                                </TextBlock>
                            </StackPanel>
                        </StackPanel>

                        <StackPanel Grid.Column="1"
                                    HorizontalAlignment="Right"
                                    Spacing="4"
                                    VerticalAlignment="Center">
                            <TextBlock Text="{Binding Item.Rarity}"
                                       FontSize="12"
                                       FontWeight="SemiBold"
                                       HorizontalAlignment="Right"/>
                            <TextBlock FontSize="12"
                                       HorizontalAlignment="Right">
                                <Run Text="x"/><Run Text="{Binding Quantity}"/>
                            </TextBlock>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

    </Grid>
</Page>

<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="DreamScape.Pages.Account.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DreamScape.Pages.Account"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Back button -->
        <Button x:Name="backButton"
                Grid.Row="0"
                Margin="12,12,0,0"
                Click="backButton_Click"
                Style="{StaticResource NavigationBackButtonNormalStyle}"
                IsEnabled="{x:Bind Frame.CanGoBack, Mode=OneWay}"/>

        <!-- Login form centered on the page -->
        <StackPanel Grid.Row="1"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Spacing="16"
                    Width="320">

            <TextBlock Text="DreamScape"
                       Style="{StaticResource TitleLargeTextBlockStyle}"
                       HorizontalAlignment="Center"/>

            <TextBlock Text="Inloggen"
                       Style="{StaticResource SubtitleTextBlockStyle}"
                       HorizontalAlignment="Center"/>

            <TextBox x:Name="NaamTextBox"
                     PlaceholderText="Gebruikersnaam"
                     Header="Naam"/>

            <PasswordBox x:Name="WachtwoordBox"
                         PlaceholderText="Wachtwoord"
                         Header="Wachtwoord"
                         KeyDown="WachtwoordBox_KeyDown"/>

            <Button x:Name="LoginButton"
                    Content="Inloggen"
                    HorizontalAlignment="Stretch"
                    Style="{StaticResource AccentButtonStyle}"
                    Click="Login_Click"/>

            <Button x:Name="OwnerLoginButton"
                    Content="Inloggen als Eigenaar"
                    HorizontalAlignment="Stretch"
                    Click="OwnerLoginButton_Click"/>

        </StackPanel>
    </Grid>
</Page>
